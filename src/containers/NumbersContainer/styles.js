import styled from 'styled-components';

const StyledNumbersContainer = styled.div`
	display: grid;
	grid-template-columns: repeat(3, 1fr);
	grid-gap: 20px;
	align-items: center;
`;

export default StyledNumbersContainer;
